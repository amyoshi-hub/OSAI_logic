改変検知はパラメーターとにらめっこするしかない

あとは各ホストがホップ数で隣のホストを管理すればやがて全体では管理できるし
局所のネットワークも達成しうる

例えば何個のホップ数が最適とかは実際動かしてみる

あと監視するのはどれだけの変化を許容するのか
これは成長率でもある

あとはまともに動くのは何個のホストがいるか

forward型のAIも教師との比較はいい性能ANDなら特に出るから問題はない
